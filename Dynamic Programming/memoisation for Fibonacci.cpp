#include <bits/stdc++.h>
using namespace std;

// Normal way of Using Recursion to Calculate Fibonacci

/*
    int fibo(int num)
{
    if (num <= 1)
        return num;
    return fibo(num - 1) + fibo(num - 2);
}

int main()
{
    int num;
    cin >> num;
    cout << fibo(num);
    return 0;
}
*/
